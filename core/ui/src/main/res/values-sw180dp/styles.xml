<resources>
    <!-- 手表窄屏档（sw<320dp）：按钮字号从 11dp 提升为 12sp 增强可读性，边距压缩 -->
    <style name="ButtonSmallFontStyle" parent="GrayButton">
        <item name="android:letterSpacing">0</item>
        <item name="android:paddingTop">0dp</item>
        <item name="android:paddingStart">0dp</item>
        <item name="android:paddingEnd">0dp</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="ButtonMediumFontStyle" parent="GrayButton">
        <item name="android:textSize">13sp</item>
    </style>

    <dimen name="gray_material_button_margin_horizontal">1dp</dimen>
</resources>
